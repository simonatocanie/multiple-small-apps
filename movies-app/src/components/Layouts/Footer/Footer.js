
import React from 'react';
import classes from './Footer.module.css';

const Footer = props => {
    console.log('Header')
    return (
        <footer>
            footer
        </footer>
    )
}

export default React.memo(Footer);