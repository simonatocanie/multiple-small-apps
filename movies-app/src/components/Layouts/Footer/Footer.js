
import React from 'react';
import classes from './Footer.module.css';

const Footer = props => {
    console.log('Footer')
    return (
        <footer>
            footer
        </footer>
    )
}

export default React.memo(Footer);